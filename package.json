{
  "name": "root",
  "private": true,
  "scripts": {
    "repo": "npm run bootstrap && npm run build",
    "bootstrap": "lerna bootstrap --ci",
    "build": "lerna run build",
    "demo": "lerna run demo",
    "clean": "lerna run clean"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "npm": "^6.14.8"
  }
}
