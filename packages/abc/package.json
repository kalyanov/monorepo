{
  "name": "@kalyanov/abc",
  "version": "1.0.0",
  "license": "ISC",
  "main": "dist/index.js",
  "files": [
    "dist/index.js"
  ],
  "dependencies": {
    "@kalyanov/ab": "1.0.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp lib.js dist/index.js",
    "demo": "node demo.js",
    "clean": "rm -rf node_modules && rm -rf dist"
  }
}
